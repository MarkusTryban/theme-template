<!DOCTYPE html>
<html>
  <head>
    {{ content_for_header }}
    {{- 'styles.css' | asset_url | stylesheet_tag -}}
  </head>
  <body>
    {%- section 'header' -%}
    {{ content_for_layout }}
    {%- section 'footer' -%}
    {{- 'theme.js' | asset_url | script_tag -}}
  </body>
</html>
